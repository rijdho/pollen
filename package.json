{
  "name": "pollen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Live audience questions with no accounts and no tracking, on Cloudflare Workers.",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "npm test && wrangler deploy",
    "live": "node tests/live.mjs",
    "screenshots": "node docs/screenshots.mjs",
    "ui": "node tests/ui.mjs",
    "serve": "node server/index.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.81.1"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
